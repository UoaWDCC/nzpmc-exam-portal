scalar DateTime

type Query {
    me: User
    user(userID: ID!): User 
    users: [User]!
    question(questionID: ID!): Question
    questions: [Question]
    quizzes: [Quiz]
    userQuizzes: [UserQuiz]
    userQuiz(quizID: ID!): UserQuiz
}
