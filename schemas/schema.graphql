scalar DateTime

type Query {
    me: User
    user(userID: ID!): User 
    users: [User]
    question(questionID: ID!): Question
    questions: [Question]
    quizzes: [Quiz]
    userQuiz: UserQuiz
    userQuizAnswers(quizID: ID!): [UserQuizAnswer]
}
