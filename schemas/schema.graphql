scalar DateTime

type Query {
    """
    General
    """
    me: User

    """
    Admin
    """
    user(userID: ID!): User
    users: [User]!

    quizzes: [Quiz]

    """
    User
    """
    userQuizzes: [UserQuiz]
    userQuiz(quizID: ID!): UserQuiz
}
